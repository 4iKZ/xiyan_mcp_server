# 2025-02-03 开发总结

## 任务目标
修复 CockroachDB 配置问题，确保 Schema 过滤正确识别并查询 CockroachDB 相关的表，而不是返回 sundb 的数据。

## 问题分析

### 1. 现象描述
配置文件中配置的是 CockroachDB，但查询结果返回的是 sundb 相关的数据：
```yaml
database:
  system: "CockroachDB"  # 配置的是 CockroachDB
  dialect: "greptimedb"
```

查询 "CPU使用率" 时，返回的是 `sundb_metrics.sys_cpu_usage` 的数据，而不是 CockroachDB 的数据。

### 2. 根本原因
经过详细排查，发现了三个层面的不一致：

#### a) 数据库层面
通过 psql 查询发现，数据库中的 schema 名称是**全小写**的：
```sql
SELECT DISTINCT table_schema FROM information_schema.tables;

结果：
- cockroach_logs
- cockroach_metrics
- cockroach_traces
- sundb_logs
- sundb_metrics
```

#### b) Redis 知识库层面
Redis 中的 `database` 字段值是**首字母大写**的：
```json
{
  "database": "CockroachDB_metrics",
  "table_name": "sys_cpu_sys_percent"
}
```

#### c) 配置层面
配置文件中 `system` 字段是**首字母大写**的：
```yaml
database:
  system: "CockroachDB"
```

### 3. 问题链路
```
配置 system: "CockroachDB"
    ↓
GreptimeDBSource 查询: table_schema LIKE 'CockroachDB%'
    ↓
无法匹配 'cockroach_metrics'（大小写不匹配）
    ↓
_usable_tables = []  (空列表)
    ↓
mschema 中没有 CockroachDB 相关的表
    ↓
Schema 过滤返回的表名无法在 mschema 中匹配
    ↓
build_sub_schema() 返回 0 个表
    ↓
模型使用错误的表名生成 SQL
```

## 已完成的修复

### 1. 配置文件修复
**文件**: `src/xiyan_mcp_server/config.yml`

**修改前**:
```yaml
database:
  system: "CockroachDB"
  dialect: "greptimedb"
```

**修改后**:
```yaml
database:
  system: "cockroach"
  dialect: "greptimedb"
```

**原因**: 使 `system` 前缀与数据库中的实际 schema 名称（`cockroach_*`）匹配。

### 2. Redis 知识库重建
**操作**: 删除旧索引，使用新的 JSON 文件名重新索引

**文件名变更**:
- `CockroachDB_logs_knowledge.json` → `cockroach_logs_knowledge.json`
- `CockroachDB_metrics_knowledge.json` → `cockroach_metrics_knowledge.json`
- `CockroachDB_traces_knowledge.json` → `cockroach_traces_knowledge.json`

**重建命令**:
```bash
redis-cli FT.DROPINDEX xiyan_schema DD
PYTHONPATH=src:$PYTHONPATH python scripts/index_knowledge.py --config src/xiyan_mcp_server/config.yml
```

**结果**: Redis 中的 `database` 字段现在是小写的（`cockroach_logs`, `cockroach_metrics`, `cockroach_traces`），与数据库 schema 一致。

### 3. Schema 过滤代码修复

#### a) `_get_matched_database_tags()` - 大小写不敏感匹配
**文件**: `src/xiyan_mcp_server/utils/schema_retriever.py:48-66`

**修改内容**:
```python
def _get_matched_database_tags(self, prefix: str) -> List[str]:
    """从 Redis 获取所有匹配前缀的 database 标签"""
    # 使用 FT.TAGVALS 获取所有 database 字段的值
    all_tags = self.redis.execute_command("FT.TAGVALS", self.index_name, "database")

    # 使用不区分大小写的前缀匹配
    prefix_lower = prefix.lower()
    matched = [
        tag.decode() if isinstance(tag, bytes) else tag
        for tag in all_tags
        if (tag.decode() if isinstance(tag, bytes) else tag).lower().startswith(prefix_lower)
    ]
    return matched
```

**原因**: Redis TAG 字段存储的标签值是小写的，需要不区分大小写匹配。

#### b) `retrieve_table_names()` - database 字段转小写
**文件**: `src/xiyan_mcp_server/utils/schema_retriever.py:179-181`

**修改内容**:
```python
results = self.retrieve(query, database, system_prefix, top_k)
# 将 database 字段转换为小写，与数据库 schema 保持一致
return [f"{item['database'].lower()}.{item['table_name']}" for item in results]
```

**原因**: Redis 返回的 `database` 字段可能是首字母大写的（如 `CockroachDB_metrics`），需要转换为小写（`cockroach_metrics`）才能与 mschema 中的表名匹配。

#### c) `build_sub_schema()` - 大小写不敏感表名匹配
**文件**: `src/xiyan_mcp_server/utils/schema_retriever.py:188-220`

**修改内容**:
```python
def build_sub_schema(self, table_names: List[str]) -> str:
    # 创建大小写不敏感的映射表
    mschema_tables_lower = {k.lower(): k for k in self.mschema.tables.keys()}

    # 只复制检索到的表（使用大小写不敏感匹配）
    matched_count = 0
    for table_name in table_names:
        table_name_lower = table_name.lower()
        if table_name_lower in mschema_tables_lower:
            actual_table_name = mschema_tables_lower[table_name_lower]
            table_info = self.mschema.tables[actual_table_name]
            matched_count += 1
            # 添加表到 sub_mschema
            ...
```

**原因**: 确保即使表名大小写不完全一致，也能正确匹配。

## 修改文件清单

| 文件 | 修改内容 |
|------|---------|
| `src/xiyan_mcp_server/config.yml` | `system: "CockroachDB"` → `system: "cockroach"` |
| `src/xiyan_mcp_server/utils/schema_retriever.py` | 3 处大小写不敏感匹配逻辑 |
| `json/cockroach_logs_knowledge.json` | 文件重命名 |
| `json/cockroach_metrics_knowledge.json` | 文件重命名 |
| `json/cockroach_traces_knowledge.json` | 文件重命名 |

## 遗留问题

### 1. ModelScope API 响应慢/速率限制

#### 现象
```
2026-02-03 10:41:05,401 - xiyan_mcp_server - INFO - 全局数据库引擎已初始化
```

服务在此之后卡住，没有后续日志，查询超时。

#### 原因
- ModelScope API 对 32B 模型有严格的速率限制
- 错误信息：`Error code: 429 - We have to rate limit you for model XGenerationLab/XiYanSQL-QwenCoder-32B-2504`
- API 响应时间过长，超过 60 秒超时

#### 解决方案

**方案 1：使用本地模型（推荐）**
```bash
# 1. 下载模型
modelscope download --model XGenerationLab/XiYanSQL-QwenCoder-3B-2502

# 2. 启动本地服务
cd /data/xiyan_mcp_server
python src/xiyan_mcp_server/local_model/local_xiyan_server.py

# 3. 修改配置指向本地服务
model:
  name: "XGenerationLab/XiYanSQL-QwenCoder-3B-2502"
  url: "http://127.0.0.1:5090"
```

**方案 2：等待 API 速率限制恢复**
- 速率限制通常在 15-30 分钟后自动解除
- 需要减少请求频率

**方案 3：使用其他 API 提供商**
- 考虑使用其他 OpenAI 兼容的 API 端点
- 确保支持 32B 模型且有足够的配额

### 2. CSV 格式化 Bug

#### 现象
CSV 格式输出时，每个字符单独一行：
```
(
p
s
y
c
o
p
g
2
...
```

#### 原因
`format_result()` 函数中 CSV 格式化逻辑有问题。

#### 修复位置
`src/xiyan_mcp_server/server.py` 中的 `format_result()` 方法

### 3. 验证任务

由于 API 问题未完成的功能验证：

- [ ] 验证 CockroachDB Schema 过滤是否正常工作
- [ ] 测试查询是否返回 CockroachDB 相关的表
- [ ] 确认大小写不敏感匹配是否生效
- [ ] 验证 Sub-Schema 构建是否成功

建议在 API 恢复或使用本地模型后重新测试。

## 技术要点总结

### 1. GreptimeDB 的 schema 命名
- GreptimeDB 使用 `catalog.schema.table` 三层结构
- 在这个环境中，catalog 名也是 schema 名
- schema 名称是**小写**的：`cockroach_metrics`, `sundb_metrics`

### 2. Redis Search TAG 字段
- TAG 字段的值是**不区分大小写**的
- `FT.TAGVALS` 返回的标签值会被转换为小写
- 但原始文档中的字段值保持原始大小写

### 3. 大小写匹配策略
- 数据库查询：使用 LIKE 匹配，区分大小写
- Redis TAG 匹配：TAG 值是小写的，需要不区分大小写匹配
- 表名匹配：需要不区分大小写匹配，因为来源不同（数据库 vs Redis）

### 4. Schema 过滤流程
```
用户查询 "CPU使用率"
    ↓
1. 生成查询向量 (Embedding API)
    ↓
2. Redis 向量搜索 (使用 system_prefix 过滤)
    ↓
3. 返回相关表列表 (database.table_name 格式)
    ↓
4. database 字段转小写
    ↓
5. 从完整 mschema 中提取匹配的表 (大小写不敏感)
    ↓
6. 构建 Sub-Schema
    ↓
7. 传递给模型生成 SQL
```

## 后续工作建议

1. **优先级 1**：解决 API 响应问题
   - 部署本地模型或切换 API 提供商
   - 完成功能验证测试

2. **优先级 2**：修复 CSV 格式化 Bug
   - 检查 `format_result()` 方法
   - 添加 CSV 格式测试

3. **优先级 3**：添加性能监控
   - 记录 API 响应时间
   - 添加超时告警

4. **优先级 4**：完善测试用例
   - 添加 CockroachDB 专项测试
   - 验证大小写不敏感匹配

## 相关文件

- 配置文件：`src/xiyan_mcp_server/config.yml`
- Schema 过滤：`src/xiyan_mcp_server/utils/schema_retriever.py`
- GreptimeDB 数据源：`src/xiyan_mcp_server/utils/greptimedb_source.py`
- 知识库索引脚本：`scripts/index_knowledge.py`
- 知识库文件：`json/cockroach_*.json`

## 参考资料

- [GreptimeDB 文档](https://docs.greptime.com/)
- [Redis Search TAG 字段](https://redis.io/docs/stack/search/reference/tags/)
- [XiYanSQL 项目](https://github.com/XGenerationLab/XiYanSQL-QwenCoder)

---
**最后更新**: 2025-02-03
**状态**: Schema 过滤修复完成，等待 API 验证
